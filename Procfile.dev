rails:   cd backend && bundle exec rails server -p 3001 -e development
next:    export PATH="$HOME/.nodebrew/current/bin:$PATH" && cd frontend && npm run dev -- --port 4000
# sidekiq: cd backend && bundle exec sidekiq -C config/sidekiq.yml  # Phase 2 で有効化
